# dotfiles

some dot files

## Commands

Install all available macOS updates:

    softwareupdate -i -a

Install Xcode Command Line Tools:

    xcode-select --install

Install rosetta 2 (for running intel apps on silicon macs):

    softwareupdate --install-rosetta --agree-to-license
